float dividend  = 11;
float divisor = 2;
float remainder = dividend % divisor;
float quotient = dividend / divisor;

Console.WriteLine($"{dividend} | {divisor} -> quotient = {(int)quotient} -> remainder = {remainder}");

